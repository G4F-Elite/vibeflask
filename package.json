{
  "name": "byte-regent-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Satirical raw-HTTP lab where GPT receives request bytes and writes response bytes by hand.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
